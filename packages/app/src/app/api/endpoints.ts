// --------------------------------------------------------------------- 
// Api > Endpoints
// --------------------------------------------------------------------- 

// --------------------------------------------------------------------- 
import { EndpointsArgs } from './types';
// --------------------------------------------------------------------- 

// --------------------------------------------------------------------- 
export type EndpointName = 'getData';
// --------------------------------------------------------------------- 

const endpoints: Record<EndpointName, EndpointsArgs> = {
	getData: {
		url: '/breakerino/data',
		method: 'GET'
	},
};

export default endpoints;
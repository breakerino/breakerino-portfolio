// --------------------------------------------------------------------- 
// Config > Plugins
// --------------------------------------------------------------------- 

export default ({ env }) => ({
	'strapi-cache': {
		enabled: true,
	},
});
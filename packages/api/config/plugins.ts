// --------------------------------------------------------------------- 
// Config > Plugins
// --------------------------------------------------------------------- 

export default ({ env }) => ({
	'wysiwyg-react-md-editor': {
		enabled: true,
		config: {
			toolbarCommands: ['title1', 'strapiMediaLibrary'],
		},
	}
});
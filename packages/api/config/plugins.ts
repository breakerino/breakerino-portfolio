// --------------------------------------------------------------------- 
// Config > Plugins
// --------------------------------------------------------------------- 

export default ({ env }) => ({
  'users-permissions': {
    config: {
      jwtSecret: env('API_JWT_SECRET'),
    },
  },
});